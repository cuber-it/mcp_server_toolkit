# MCP Server Factory — Example Configuration
server_name: "MCP Factory"
transport: stdio
log_level: INFO

plugins:
  echo:
    enabled: true
  # wekan:
  #   enabled: true
  #   url: "http://localhost:12050"
  # shell:
  #   enabled: true
