# MCP Proxy — stdio for Claude Code / Claude Desktop
# No OAuth, no ports, no health server.
server_name: "MCP Proxy (stdio)"
transport: stdio
log_level: WARNING

autoload:
  - echo
  # Production plugins (install from PyPI, load via dotted import):
  # - mcp_shell_tools.shell
  # - mcp_wekan_tools.wekan
